spring:
  cloud:
    config:
      discovery:
        enabled: true
        service-id: springcloud-service-provider
eureka:
  client:
    service-url:
      defaultZone: http://localhost:12000/eureka